[project]
name = "yoloe-mode-lab"
version = "0.1.0"
description = "Local playground for YOLOE text, visual, and prompt-free modes"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
  "ultralytics>=8.4.0",
  "gradio>=5.49,<7",
  "opencv-python>=4.10",
  "numpy>=1.26",
  "Pillow>=10.4",
]

[tool.uv]
package = false
